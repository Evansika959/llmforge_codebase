"""LLMForge: hardware-aware architecture search for edge language models.

Subpackages
    llmforge.search      search spaces, NSGA-II, and the co-search dispatcher
    llmforge.supernet    elastic supernets over pretrained checkpoints (software evaluator)
    llmforge.evaluators  software and hardware evaluator backends
    llmforge.hw          hardware targets: ZEUS (GPU), Timeloop and rDXE (simulators), device
"""

__version__ = "0.2.0"
